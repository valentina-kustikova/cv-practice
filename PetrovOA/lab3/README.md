# Лабораторная работа №3. Классификация изображений архитектурных объектов

## Цель работы

Разработка и сравнение алгоритмов компьютерного зрения для автоматической классификации фотографий архитектурных достопримечательностей Нижнего Новгорода.

## Реализованные методы

В рамках работы реализованы **два различных подхода** к задаче классификации:

1.  **Классический метод машинного обучения**: Bag of Words (BoW) с использованием SIFT-дескрипторов, кластеризации K-Means и классификатора SVM.
2.  **Современный подход глубокого обучения**: Vision Transformer (ViT-B/16) на базе фреймворка PyTorch с предобученными весами ImageNet-1K.

### Распознаваемые объекты:
- **01_NizhnyNovgorodKremlin** - Нижегородский Кремль
- **04_ArkhangelskCathedral** - Архангельский собор  
- **08_PalaceOfLabor** - Дворец труда

---

## 1. Классический подход: Bag of Words (BoW)

### Принцип работы:

#### 1.1 Детектирование локальных признаков
- Применение алгоритма **SIFT** (Scale-Invariant Feature Transform) для поиска характерных точек на изображении
- Формирование 128-мерных векторов-дескрипторов для каждой обнаруженной точки
- Обеспечение инвариантности к масштабированию, вращению и частичным изменениям освещенности

#### 1.2 Формирование визуального словаря
- Агрегация дескрипторов со всех изображений обучающей выборки
- Применение алгоритма **K-means** для разбиения дескрипторов на кластеры (по умолчанию: 100 кластеров)
- Использование центроидов кластеров в качестве "визуальных слов" словаря

#### 1.3 Векторизация изображений
- Сопоставление каждого дескриптора изображения с ближайшим "визуальным словом"
- Подсчет частоты встречаемости каждого слова
- Нормализация полученной гистограммы (приведение суммы к единице)

#### 1.4 Обучение классификатора
- Применение метода опорных векторов **SVM** с RBF-ядром
- Обучение на векторизованных представлениях изображений
- Классификация новых объектов на основе обученной модели

### Конфигурация алгоритма:
-   **Детектор**: `cv2.SIFT_create()` с параметрами по умолчанию
-   **Число кластеров K-Means**: `n_clusters=100` (настраивается параметром `--clusters`)
-   **Классификатор**: `SVC(kernel='rbf', probability=True)`

### Достоинства BoW:
-  Устойчивость к геометрическим преобразованиям (масштаб, поворот)
-  Работа на центральном процессоре без требований к GPU
-  Прозрачность и интерпретируемость решений
-  Эффективность на ограниченных наборах данных

### Ограничения BoW:
-  Игнорирование пространственного расположения признаков
-  Необходимость ручного выбора типа дескриптора
-  Уступает в точности современным нейросетевым архитектурам

---

## 2. Современный подход: Vision Transformer (ViT)

## 2. Современный подход: Vision Transformer (ViT)

### Принцип работы:

#### 2.1 Архитектурное решение
- Базовая модель: **ViT-B/16** (Vision Transformer) из библиотеки torchvision
- Предобучение на датасете ImageNet-1K (1.28 млн изображений, 1000 категорий)
- Архитектура: замороженная базовая сеть (backbone) + обучаемая классификационная голова
- Реализация на платформе PyTorch

#### 2.2 Процесс извлечения признаков
- Нормализация входных изображений до разрешения 224×224 пикселей
- Извлечение 768-мерного эмбеддинга из выходного слоя ViT-B/16
- Использование class token - специального токена трансформера, содержащего глобальную информацию об изображении
- Эмбеддинг кодирует семантическое представление объекта

#### 2.3 Архитектура классификационной головы
- Последовательность полносвязных слоев: 768 → 512 → 256 → количество_классов
- Применение Dropout (коэффициенты 0.3 и 0.2) для предотвращения переобучения
- Активационная функция ReLU между слоями

#### 2.4 Процесс обучения
- **Обучение**: выполняется на GPU (CUDA) для ускорения вычислений
- **Инференс**: возможность работы на CPU без требований к видеокарте
- Оптимизатор Adam с адаптивным планировщиком ReduceLROnPlateau
- Аугментация данных: случайные отражения, повороты, изменения цвета

### Конфигурация модели:
- **Базовая сеть**: ViT-B/16 (заморожена) из torchvision
- **Разрешение входа**: 224×224 пикселей
- **Размер пакета**: 16 (параметр `--batch_size`)
- **Скорость обучения**: 0.001 (параметр `--learning_rate`)
- **Число эпох**: 20 (параметр `--epochs`)
- **Оптимизатор**: Adam (обучение только классификационной головы)

### Достоинства ViT:
- **Высокая точность**: достижение 95-100% на тестовых данных
- **Надежность загрузки**: использование официального репозитория PyTorch
- **Превосходная генерализация**: предобучение на обширном датасете ImageNet-1K
- **Семантическое понимание**: трансформеры учитывают контекстные связи в изображении
- **Модульность**: простота замены базовой сети на альтернативные архитектуры

### Ограничения ViT:
- Необходимость GPU для эффективного обучения
- Расширенный список зависимостей (PyTorch, torchvision)
- Увеличенное время инференса на CPU по сравнению с классическими методами

### Дополнительная информация:
📘 Подробное руководство по работе с ViT классификатором: **[VIT_GUIDE.md](VIT_GUIDE.md)**

---

## 3. Структура проекта

```
lab3/
├── classifier/              # Пакет с классификаторами
│   ├── __init__.py
│   ├── base_classifier.py  # Базовый класс с общей логикой
│   ├── bow_classifier.py   # Реализация алгоритма "мешок слов"
│   └── vit_classifier.py   # Реализация Vision Transformer
├── data/                   # Директория с данными
│   ├── train.txt          # Список обучающих изображений
│   ├── test.txt           # Список тестовых изображений
│   ├── ExtDataset/        # Внешний датасет
│   └── NNSUDataset/       # Датасет ННГУ
├── bow_model/             # Сохраненные модели BOW классификатора
│   └── bow_model.joblib   # Модель (K-means + SVM + метаданные)
├── vit_model/             # Сохраненные модели ViT классификатора
│   ├── vit_model.pth      # Модель PyTorch
│   └── metadata.json      # Метаданные (class_names, image_size, etc.)
├── main.py                # Основной скрипт для запуска
├── README.md              # Документация
├── VIT_GUIDE.md           # Подробное руководство по ViT
└── requirements_pytorch.txt  # Зависимости для PyTorch
```

---

## 4. Установка и требования

### Необходимые библиотеки:

**Базовые зависимости (для BoW):**
```bash
pip install opencv-python
pip install numpy
pip install scikit-learn
pip install joblib
```

**Для ViT (дополнительно):**
```bash
# Для CPU (инференс):
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Для GPU (обучение, CUDA 11.8):
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Остальные зависимости:
pip install -r requirements_pytorch.txt
```

### Требования к системе:
- Python 3.7+
- **Для ViT: GPU рекомендуется для обучения, CPU достаточно для инференса**

---

## 5. Использование

### 5.1 Входные параметры

| Параметр | Описание | Обязательный | По умолчанию |
|----------|----------|--------------|--------------|
| `--data_dir` | Путь к директории с изображениями | Да | - |
| `--train_file` | Путь к файлу со списком обучающих изображений | Да | - |
| `--test_file` | Путь к файлу со списком тестовых изображений | Да | - |
| `--mode` | Режим работы: `train`, `test`, `both` | Нет | `both` |
| `--algorithm` | Алгоритм: `bow`, `vit`, `both` | Нет | `both` |
| `--model_dir` | Директория для сохранения/загрузки моделей | Нет | `bow_model` или `vit_model` |
| `--clusters` | Количество кластеров для BoW | Нет | `100` |
| `--learning_rate` | Скорость обучения для ViT | Нет | `0.001` |
| `--batch_size` | Размер батча для ViT | Нет | `16` |
| `--epochs` | Количество эпох для ViT | Нет | `20` |
| `--visualize` | Путь к изображению для визуализации SIFT-ключевых точек | Нет | - |
| `--visualize_output` | Путь для сохранения изображения с визуализацией | Нет | - |
| `--visualize_style` | Стиль отрисовки: `rich`, `simple`, `not_scaled` | Нет | `rich` |

### 5.2 Команды запуска

**Важно:** Перед первым запуском создайте train/test split для вашего датасета:

```bash
# Создание train/test split (80/20) для NNSUDataset
python create_split.py
```

Это создаст файлы `data/train_nnsu.txt` и `data/test_nnsu.txt`.

#### Тестирование BOW классификатора (с готовой моделью):

```bash
python main.py --data_dir data --train_file data/train.txt --test_file data/test.txt --mode test --algorithm bow
```

#### Тестирование ViT классификатора (с готовой моделью):

```bash
python main.py --data_dir data --train_file data/train_nnsu.txt --test_file data/test_nnsu.txt --mode test --algorithm vit
```

#### Тестирование обоих классификаторов:

```bash
python main.py --data_dir data --train_file data/train_nnsu.txt --test_file data/test_nnsu.txt --mode test --algorithm both
```

#### Обучение и тестирование BOW классификатора:

```bash
python main.py --data_dir data --train_file data/train.txt --test_file data/test.txt --mode both --algorithm bow --clusters 100
```

#### Обучение и тестирование ViT классификатора:

```bash
python main.py --data_dir data --train_file data/train_nnsu.txt --test_file data/test_nnsu.txt --mode both --algorithm vit --learning_rate 0.001 --batch_size 16 --epochs 20
```

**📘 Для подробных инструкций по ViT см. [VIT_GUIDE.md](VIT_GUIDE.md)**

#### Визуализация SIFT-ключевых точек:

```bash
# Показать визуализацию на экране
python main.py --visualize "data/ExtDataset/01_NizhnyNovgorodKremlin/DSC_3184.jpg"

# Сохранить визуализацию в файл
python main.py --visualize "data/ExtDataset/01_NizhnyNovgorodKremlin/DSC_3184.jpg" --visualize_output "sift_visualization.jpg" --visualize_style rich
```

### 5.3 Формат файлов данных

Файлы `train.txt` и `test.txt` содержат относительные пути к изображениям, по одному на строку:

```
ExtDataset/01_NizhnyNovgorodKremlin/image1.jpg
NNSUDataset/04_ArkhangelskCathedral/image2.jpg
...
```

Пути указываются относительно директории, указанной в `--data_dir`.

### 5.4 Визуализация SIFT-ключевых точек

Для визуализации этапов работы алгоритма "мешок слов" можно использовать режим визуализации:

**Параметры визуализации:**
- `--visualize` - путь к изображению для анализа
- `--visualize_output` - путь для сохранения результата (опционально)
- `--visualize_style` - стиль отрисовки:
  - `rich` - полная визуализация с размерами и ориентацией (по умолчанию)
  - `simple` - простая отрисовка только точек
  - `not_scaled` - точки без масштабирования

**Пример использования:**
```bash
# Показать на экране
python main.py --visualize "data/ExtDataset/01_NizhnyNovgorodKremlin/DSC_3184.jpg"

# Сохранить в файл
python main.py --visualize "data/ExtDataset/01_NizhnyNovgorodKremlin/DSC_3184.jpg" --visualize_output "result.jpg"
```

```bash
=== Визуализация SIFT-ключевых точек ===

=== Анализ SIFT-дескрипторов для DSC_3184.jpg ===
Найдено ключевых точек: 1014
Средний размер области: 3.74 пикселей
Средний угол ориентации: 177.05 градусов
Средняя сила отклика: 0.0291
Размерность дескрипторов: (1014, 128)

Нажмите любую клавишу для закрытия окна...
```

При визуализации выводится статистика:
- Количество найденных ключевых точек
- Средний размер области дескриптора
- Средний угол ориентации
- Средняя сила отклика
- Размерность дескрипторов


---

## 6. Описание классов и методов

### 6.1 BaseClassifier

Базовый абстрактный класс, содержащий общую логику для всех классификаторов.

**Основные методы:**
- `detect_label_from_path(image_path)` - определяет метку класса из пути к файлу
- `load_data(data_file, data_dir)` - загружает данные из файла со списком изображений
- `load_image(image_path)` - загружает и предобрабатывает изображение
- `evaluate(y_true, y_pred, target_names)` - оценка качества классификации
- `train(train_paths, train_labels)` - абстрактный метод для обучения
- `test(test_paths, test_labels)` - абстрактный метод для тестирования

### 6.2 BOWClassifier

Реализация классификатора на основе метода "мешок слов".

**Основные методы:**
- `extract_features(image)` - извлекает SIFT-дескрипторы из изображения
- `build_vocabulary(all_descriptors)` - строит визуальный словарь с помощью K-means
- `descr_to_histogram(descriptors)` - преобразует дескрипторы в гистограмму визуальных слов
- `train(train_paths, train_labels)` - обучает модель BOW на предоставленных данных
- `test(test_paths, test_labels)` - тестирует модель на предоставленных данных
- `show_keypoints(image_path, output_path, draw_style)` - визуализирует SIFT-ключевые точки на изображении
- `save_model()` - сохраняет модель в файл `bow_model.joblib`
- `load_model()` - загружает модель из файла

**Сохранение модели:**
- Все данные сохраняются в одном файле `bow_model.joblib`:
  - `kmeans` - модель кластеризации (визуальный словарь)
  - `classifier` - SVM классификатор
  - `class_names` - названия классов
  - `n_clusters` - количество кластеров

### 6.3 CNNClassifier

Реализация классификатора на основе сверточной нейронной сети VGG16.

**Основные методы:**
- `create_model(n_classes)` - создает модель CNN на основе VGG16 с transfer learning
- `preprocess_image(image)` - предобрабатывает изображение для CNN
- `train(train_paths, train_labels)` - обучает CNN модель на предоставленных данных
- `test(test_paths, test_labels)` - тестирует CNN модель на предоставленных данных
- `save_model()` - сохраняет модель и метаданные
- `load_model()` - загружает модель и метаданные

**Сохранение модели:**
- `cnn_model.h5` - архитектура и веса нейросети (формат Keras)
- `metadata.npy` - метаданные:
- `class_names` - названия классов
- `label_encoder` - кодировщик меток (LabelEncoder)
- `image_size` - размер входных изображений

---

## 7. Результаты экспериментов

### 7.1 Классический метод Bag of Words (BoW)

**Общая точность: 95.6%**

| Класс | Precision | Recall | F1-score | Support |
|-------|-----------|--------|----------|---------|
| Нижегородский Кремль | 0.96 | 1.00 | 0.98 | 22 |
| Архангельский собор | 0.90 | 0.90 | 0.90 | 10 |
| Дворец труда | 1.00 | 0.92 | 0.96 | 13 |
| **Среднее (macro avg)** | 0.95 | 0.94 | 0.95 | 45 |
| **Среднее (weighted avg)** | 0.96 | 0.96 | 0.96 | 45 |

**Интерпретация результатов:**
- Наилучшие показатели: Нижегородский Кремль (F1=0.98, идеальный recall=1.00)
- Хорошие результаты: Дворец труда (F1=0.96, отличный precision=1.00)
- Приемлемые показатели: Архангельский собор (F1=0.90)
- Модель успешно распознает все изображения Кремля, не пропуская ни одного

### 7.2 Современный метод Vision Transformer (ViT)

**Общая точность: 100.0%**

| Класс | Precision | Recall | F1-score | Support |
|-------|-----------|--------|----------|---------|
| Нижегородский Кремль | 1.00 | 1.00 | 1.00 | 22 |
| Архангельский собор | 1.00 | 1.00 | 1.00 | 10 |
| Дворец труда | 1.00 | 1.00 | 1.00 | 13 |
| **Среднее (macro avg)** | 1.00 | 1.00 | 1.00 | 45 |
| **Среднее (weighted avg)** | 1.00 | 1.00 | 1.00 | 45 |

**Интерпретация результатов:**
- Безошибочная классификация всех объектов из тестовой выборки
- Идеальные метрики по всем трем классам (precision, recall, f1-score = 1.00)
- Значительное улучшение по сравнению с BoW для класса "Архангельский собор" (+0.10 F1)
- Предобученная сеть ViT-B/16 обеспечивает мощные признаки высокого уровня

### 7.3 Сравнительный анализ методов

| Метод | Точность | Время обучения | Преимущества | Недостатки |
|-------|----------|----------------|--------------|------------|
| **BoW + SVM** | 95.6% | ~2-3 мин (CPU) | Работа без GPU, высокая скорость, прозрачность решений | Ниже точность, чувствительность к сложным объектам |
| **ViT (ViT-B/16)** | 100.0% | ~5-7 мин (GPU) | Максимальная точность, стабильная загрузка через torchvision | Требования к GPU, дополнительные зависимости |

**Выводы:**
- ViT превосходит BoW на 4.4 процентных пункта по общей точности
- Максимальное улучшение достигнуто для класса "Архангельский собор": повышение F1-score с 0.90 до 1.00
- Vision Transformer эффективнее извлекает семантические характеристики изображений
- ViT-B/16 из torchvision демонстрирует стабильность и воспроизводимость результатов
- Для практического применения при высоких требованиях к точности рекомендуется ViT
- Для быстрого прототипирования и работы на слабом оборудовании подходит BoW

---

## 8. Использованные библиотеки

**Для BoW:**
- **OpenCV (cv2)** - извлечение SIFT-дескрипторов, работа с изображениями
- **NumPy** - работа с массивами и матрицами
- **scikit-learn** - K-means кластеризация, SVM классификатор, метрики оценки
- **joblib** - сохранение и загрузка моделей BoW

**Для ViT:**
- **PyTorch** - фреймворк глубокого обучения
- **torchvision** - трансформации изображений и предобученные модели
- **Pillow (PIL)** - обработка изображений
- **tqdm** - прогресс-бары для обучения

---

## 9. Примечания

### О структуре данных:
- Изображения должны быть организованы в подпапках с названиями классов
- Названия классов определяются автоматически из структуры директорий
- Поддерживаются форматы: JPG, JPEG, PNG

### О моделях:
- BOW модель сохраняется в формате `joblib` (один файл со всеми данными)
- ViT модель сохраняется в формате PyTorch checkpoint (.pth) + JSON метаданные
- При загрузке модели автоматически определяются классы и параметры

### О производительности:
- **BoW:** обучение ~2-3 минуты (CPU), тестирование ~5-10 секунд
- **ViT:** обучение ~5-7 минут (GPU), тестирование ~10-15 секунд (CPU)

### О GPU:
- ViT **требует GPU** для эффективного обучения
- Инференс ViT работает на CPU (медленнее, но приемлемо)
- Проверьте наличие CUDA: `torch.cuda.is_available()`

---
