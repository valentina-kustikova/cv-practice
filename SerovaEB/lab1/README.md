# Практическая работа №1. Обработка изображений с использованием библиотеки OpenCV

Простой инструмент для обработки изображений с различными фильтрами и эффектами, реализованный на Python с использованием OpenCV и NumPy.

## 🚀 Возможности

- **📐 Изменение размера** - Масштабирование изображения 
- **🟫 Сепия** - Наложение сепии с регулируемой интенсивностью
- **🖼️ Виньетка** - Эффект затемнения краев с настройкой параметров
- **🎮 Пикселизация** - Пикселизация выбранной прямоугольной области изображения
- **🖼️ Рамки** - Добавление простых и декоративных рамок
- **✨ Блики** - Наложение текстурных бликов на изображение
- **🎨 Акварель** - Эффект акварельной живописи с текстурой бумаги

## Основные функции обработки изображений

## 1)`resize(image, new_width, new_height)`
**Назначение:** Изменение размера изображения методом ближайшего соседа

**Параметры:**
- image - исходное изображение
- new_width - новая ширина
- new_height - новая высота

**Алгоритм:**
- Вычисляет соотношения размеров по осям X и Y
- Создает координатные сетки для нового изображения
- Использует векторную индексацию NumPy для эффективного масштабирования
- Возвращает изображение с новыми размерами

## 2)`sepia(image, intensity=1.0)`
**Назначение:** Применение сепийного фильтра с регулируемой интенсивностью

**Параметры:**
- image - исходное изображение
- intensity - интенсивность эффекта (0.0 - 1.0)

**Алгоритм:**
- Преобразует изображение в вещественные числа для точных вычислений
- Разделяет цветовые каналы (BGR)
- Применяет матрицу преобразования сепии к каждому пикселю
- Регулирует интенсивность с помощью линейной интерполяции
- Ограничивает значения пикселей диапазоном 0-255

## 3)`pixel_region(image, x, y, width, height, pixel_size=10)`
**Назначение:** Пикселизация выбранной области изображения

**Параметры:**
- image - исходное изображение
- x, y - координаты левого верхнего угла области
- width, height - размеры области для пикселизации
- pixel_size - размер пикселя в блоках
- 
**Алгоритм:**
- Проверяет и корректирует границы области
= Разбивает область на блоки размером pixel_size × pixel_size
- Для каждого блока вычисляет средний цвет
- Заполняет весь блок полученным средним цветом
- Поддерживает как цветные, так и черно-белые изображения

## 4)`vignette_gaussian(image, sigma=0.3, intensity=0.8)`
**Назначение:** Создание эффекта виньетки с гауссовым распределением

**Параметры:**
- image - исходное изображение
- sigma - параметр размытости (чем больше, тем больше центральная область)
- intensity - интенсивность затемнения краев

**Алгоритм:**
- Создает координатную сетку изображения
- Вычисляет нормализованные расстояния от центра
- Создает гауссову маску с экспоненциальным затуханием
- Применяет маску к каждому цветовому каналу
- Возвращает изображение с плавным затемнением к краям

## 5)`add_border(image, border_width, color)`
**Назначение:** Добавление рамки к изображению

**Параметры:**
- image - исходное изображение
- border_width - ширина рамки в пикселях
- color - цвет рамки в формате BGR

**Алгоритм:**
- Вычисляет новые размеры изображения с учетом рамки
- Создает новое изображение, заполненное цветом рамки
- Копирует исходное изображение в центр
- Поддерживает как цветные, так и черно-белые изображения

## 6)`parse_border_color(color_str)` - вспомогальельная функция
**Назначение:** Парсинг строки цвета в кортеж BGR

**Параметры:**
- color_str - строка цвета в формате "синий,зеленый,красный"

**Алгоритм:**
- Разделяет строку по запятым
- Преобразует значения в целые числа
- Возвращает кортеж BGR
- При ошибке возвращает белый цвет по умолчанию

## 7)`bliki(image, texture, k)`
**Назначение:** Наложение текстуры бликов на изображение

**Параметры:**
- image - исходное изображение
- texture - путь к файлу текстуры
- k - коэффициент наложения

**Алгоритм:**
- Загружает текстуру из файла
- Масштабирует текстуру под размер исходного изображения
- Складывает изображение с текстурой, умноженной на коэффициент
- Ограничивает значения пикселей диапазоном 0-255

## 8)`watercolor_simple(img, texture_path, k=0.2)`
**Назначение:** Создание акварельного эффекта с текстурой бумаги

**Параметры:**
- img - исходное изображение
- texture_path - путь к текстуре бумаги
- k - коэффициент эффекта

**Алгоритм:**
- Загружает и масштабирует текстуру бумаги
- Инвертирует текстуру (255 - texture)
- Вычитает инвертированную текстуру из исходного изображения
- Создает эффект неровной поверхности бумаги

## 9)`shaped_border(image, frame_path)`
**Назначение:** Наложение декоративной рамки на изображение

**Параметры:**
- image - исходное изображение
- frame_path - путь к изображению рамки

**Алгоритм:**
- Загружает рамку с поддержкой альфа-канала
- Масштабирует рамку под размер изображения
- Создает маску из непрозрачных пикселей рамки
- Заменяет соответствующие пиксели в исходном изображении
- Для рамок без альфа-канала использует пороговое значение яркости

## `cmd_args()`
**Назначение:** Парсинг аргументов командной строки

**Параметры:** Нет

**Алгоритм:**
- Создает парсер аргументов с группами параметров для каждого фильтра
- Определяет все возможные флаги и опции
- Устанавливает значения по умолчанию
- Обрабатывает и возвращает разобранные аргументы

## Главная функция `__main__`
**Назначение:** Точка входа в программу

**Алгоритм:**
- Парсит аргументы командной строки
- Загружает исходное изображение
- Отображает оригинальное изображение
- Применяет выбранные фильтры последовательно
- Отображает результаты обработки
- Ожидает нажатия клавиши для закрытия окон

